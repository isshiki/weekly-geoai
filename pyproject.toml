[project]
name = "weekly-geoai"
version = "0.1.0"
description = "Editorial workflow for the Weekly GeoAI newsletter"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"
